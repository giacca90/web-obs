// ⚙️ Archivo autogenerado, no editar manualmente
export const AUDIO_PROCESSOR = "class t extends AudioWorkletProcessor{constructor(){super(...arguments),this.initialized=!1}static get parameterDescriptors(){return[]}process(t,s,e){this.initialized||(this.port.postMessage({event:\"worker-started\"}),this.initialized=!0);const r=t[0];if(!r||0===r.length)return!1;const o=r[0];let i=0;for(const t of o)i+=t**2;const n=Math.sqrt(i/o.length);return this.port.postMessage({rms:n}),o.length>0}}registerProcessor(\"audio-processor\",t);";
